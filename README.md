# Система управления зарядными станциями

Комплексная система для управления и мониторинга зарядных станций электромобилей с веб-интерфейсом и ESP32 прошивкой.

## Обзор проекта

Проект включает два основных компонента:
1. **Веб-приложение** - для управления и мониторинга станций через браузер
2. **ESP32 прошивка** - для работы непосредственно на устройствах зарядных станций

## Структура проекта

```
📦 Корневая папка
├── 📁 client/                    # React фронтенд
│   ├── src/
│   │   ├── components/           # UI компоненты
│   │   ├── pages/               # Страницы приложения
│   │   ├── hooks/               # React хуки
│   │   └── lib/                 # Утилиты
│   └── index.html
├── 📁 server/                    # Express.js бэкенд
│   ├── index.ts                 # Главный сервер
│   ├── routes.ts                # API маршруты
│   ├── storage.ts               # Система хранения данных
│   └── esp32-client.ts          # Клиент для ESP32
├── 📁 shared/                    # Общие схемы данных
│   └── schema.ts                # TypeScript схемы
├── 📁 data/                      # Данные приложения
│   └── stations.json            # Конфигурация станций
├── 📁 test/                      # Инструменты тестирования
│   ├── board-simulator.cjs      # Симулятор ESP32 плат
│   └── run-multiple-simulators.sh
└── 📁 projectone/                # ESP32 прошивка
    ├── main/                    # Исходный код ESP32
    ├── CMakeLists.txt           # Конфигурация сборки
    └── sdkconfig.defaults       # Настройки ESP-IDF
```

## Функциональность

### Веб-приложение

**Основные возможности:**
- Управление зарядными станциями в реальном времени
- Мониторинг статуса и мощности станций
- Сканирование локальной сети для поиска ESP32 устройств
- Подключение к удаленным платам и синхронизация данных
- Настройка параметров станций (мощность, тип, имя)
- Темная и светлая темы интерфейса

**Технологии:**
- **Frontend**: React 18, TypeScript, Tailwind CSS, shadcn/ui
- **Backend**: Express.js, Node.js
- **Маршрутизация**: Wouter
- **Формы**: React Hook Form с Zod валидацией
- **Запросы**: TanStack Query
- **Хранение**: JSON файлы с файловой системой

### ESP32 прошивка

**Основные возможности:**
- Встроенный веб-интерфейс для управления станциями
- Master/Slave архитектура для распределенных систем
- UDP коммуникация между устройствами
- WiFi подключение и автоматическое переподключение
- HTTP сервер с JSON API
- Real-time мониторинг и диагностика

**Технологии:**
- **Платформа**: ESP32 с ESP-IDF
- **Язык**: C
- **Сеть**: WiFi, UDP, HTTP
- **Веб**: HTML, CSS, JavaScript встроенные в прошивку

## Быстрый старт

### Запуск веб-приложения

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Приложение доступно по адресу: http://localhost:5000
```

### Компиляция ESP32 прошивки

```bash
# Переход в папку ESP32 проекта
cd projectone

# Настройка WiFi в main/master_slave_logic.c
# Изменить WIFI_SSID и WIFI_PASS

# Компиляция
idf.py build

# Загрузка на ESP32
idf.py -p /dev/ttyUSB0 flash monitor
```

## API документация

### Веб-приложение API

**Станции:**
- `GET /api/stations` - Получить список всех станций
- `GET /api/stations/:id` - Получить конкретную станцию
- `POST /api/stations` - Создать новую станцию
- `PATCH /api/stations/:id` - Обновить станцию
- `DELETE /api/stations/:id` - Удалить станцию

**ESP32 управление:**
- `POST /api/esp32/scan` - Сканировать сеть для поиска плат
- `POST /api/esp32/connect` - Подключиться к конкретной плате
- `POST /api/esp32/:id/sync` - Синхронизировать данные с платой

### ESP32 API

**Веб-интерфейс:**
- `GET /charging-station` - Главная страница интерфейса
- `GET /charging-station.css` - Стили
- `GET /charging-station.js` - JavaScript

**API endpoints:**
- `GET /api/stations` - Получить конфигурацию станций
- `POST /api/stations/update` - Обновить конфигурацию
- `GET /api/esp32/scan` - Сканировать другие платы
- `POST /api/esp32/connect` - Подключиться к плате

## Конфигурация

### Веб-приложение

Основные настройки в `server/index.ts`:
- Порт сервера: 5000
- Хранилище данных: JSON файлы в папке `data/`
- CORS настройки для разработки

### ESP32

Настройки в `projectone/main/master_slave_logic.c`:
```c
#define WIFI_SSID      "YOUR_WIFI_NAME"
#define WIFI_PASS      "YOUR_PASSWORD"
```

Конфигурация ESP-IDF в `projectone/sdkconfig.defaults`:
- Flash размер: 4MB
- WiFi буферы и настройки
- HTTP сервер параметры
- Размеры стеков задач

## Архитектура системы

### Веб-приложение

```
Client (React) ←→ Server (Express) ←→ JSON Storage
                      ↓
               ESP32 Network Scanner
```

### ESP32 система

```
Master ESP32 ←→ UDP Communication ←→ Slave ESP32
     ↓                                    ↓
HTTP Server                        HTTP Server
     ↓                                    ↓
Web Interface                   Web Interface
```

## Тестирование

### Симуляция ESP32 плат

```bash
# Запуск одного симулятора
cd test
node board-simulator.cjs

# Запуск нескольких симуляторов
./run-multiple-simulators.sh

# Остановка симуляторов
./stop-simulators.sh
```

### Проверка ESP32 проекта

```bash
cd projectone
./test_compilation_readiness.sh
```

## Развертывание

### Production веб-приложения

```bash
# Сборка для продакшна
npm run build

# Запуск продакшн сервера
NODE_ENV=production npm start
```

### ESP32 в продакшне

1. Настроить WiFi параметры в коде
2. Скомпилировать прошивку: `idf.py build`
3. Загрузить на все ESP32 устройства
4. Настроить первое устройство как Master
5. Остальные автоматически станут Slave

## Мониторинг и логи

### Веб-приложение

Логи сервера показывают:
- HTTP запросы и их статусы
- Результаты сканирования сети
- Ошибки подключения к ESP32

### ESP32

Логи через serial монитор:
```bash
idf.py monitor
```

Показывают:
- WiFi подключение
- HTTP сервер статус
- UDP сообщения между платами
- Статус зарядных станций

## Устранение неполадок

### Веб-приложение

**Проблема**: Сервер не запускается
**Решение**: Проверить что порт 5000 свободен

**Проблема**: Не находятся ESP32 платы
**Решение**: Убедиться что устройства в той же сети

### ESP32

**Проблема**: Не компилируется
**Решение**: Запустить `test_compilation_readiness.sh`

**Проблема**: Не подключается к WiFi
**Решение**: Проверить SSID и пароль в коде

## Поддержка

Проект включает:
- Детальную документацию в каждой папке
- Примеры конфигурации
- Скрипты для тестирования
- Симуляторы для разработки

Основные файлы документации:
- `projectone/ESP32_READY_FOR_PRODUCTION.md` - Готовность ESP32
- `projectone/BUILD_INSTRUCTIONS.md` - Инструкции сборки
- `test/README.md` - Руководство по тестированию

## Лицензия

Проект разработан для управления зарядными станциями электромобилей с открытым исходным кодом.