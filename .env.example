# Server Configuration
PORT=5000
HOST=0.0.0.0
NODE_ENV=development

# For production deployment
# NODE_ENV=production

# Custom host (optional)
# HOST=localhost  # Only local access
# HOST=0.0.0.0    # Network access (default)